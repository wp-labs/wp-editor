apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wp-editor.fullname" . }}-test-connection"
  labels:
    {{- include "wp-editor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wp-editor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
